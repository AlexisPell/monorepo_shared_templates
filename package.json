{
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "test": "lerna run test --scope={@yarn-spaces/common,@yarn-spaces/server}",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "yarn-spaces"
}
